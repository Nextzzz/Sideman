[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sideman"
version = "0.1.0"
description = "An AI band member that listens and follows you in real time"
requires-python = ">=3.12"

[tool.setuptools]
packages = ["sideman"]
